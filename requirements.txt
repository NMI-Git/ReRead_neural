# Pinned to the versions this project was last verified to run under.
# Verified 2026-08-10 on macOS 15 / Apple M2 (arm64), Python 3.9.
#
#   python -m pip install -r requirements.txt
#
# TensorFlow 2.11 has no arm64 macOS wheel under the name "tensorflow"; on
# Apple Silicon the package is called "tensorflow-macos". The markers below
# pick the right one automatically, so the same file works on Intel macOS,
# Linux and Windows.
#
# Keras is not listed separately: TensorFlow 2.11 bundles Keras 2.11, and the
# models in this repository are Keras 2 .h5 files that Keras 3 cannot load.

tensorflow-macos==2.11.0 ; sys_platform == "darwin" and platform_machine == "arm64"
tensorflow==2.11.0 ; not (sys_platform == "darwin" and platform_machine == "arm64")

numpy==1.24.4
scikit-learn==1.3.2

# Optional -- only needed by plot_models.py to regenerate the architecture
# diagrams. Also requires a system Graphviz install. Uncomment if you want them.
# pydot==1.4.2
# visualkeras==0.0.2
