# --- OS / editor / interpreter noise ---
.DS_Store
.idea/
*.iml
*.xml
*__pycache__

# --- Per-run output dumps ---
# These are overwritten on every run and would otherwise produce noisy diffs.
# results/analysis.txt is kept in version control as a worked example of
# mode 1 output.
results/outfile.txt
outfile.txt
french_analysis.txt
finnish_analysis.txt

# --- Superseded exploratory scripts from the initial tutorial phase (2023-03) ---
# Predate the two-deck architecture and are not part of any published result.
localTesting.py
load_model.py
char_seq.py
char_sequences.txt
lstm_auto_enc.h5
mapping.pkl
positional_corpus.rtf

# NOTE: Everything required to run the model and reproduce the published
# analyses is deliberately tracked -- source corpora (corpora/), derived
# training data, trained models (*.h5), char mappings (*.pkl) and the
# analysis scripts. Do not add those back here; a clone must be runnable
# as-is. See README.md "Reproducing the published analyses".
